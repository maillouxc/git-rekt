apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'com.gitrekt.resort.GitRekt'

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:5.0.1'
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.4'
}
